// Contains stubs for content created at compile time
#include <string>
#include <vector>
#include <stdexcept>

namespace clw_generated{
  const std::vector<uint8_t> get_program_bytecode(std::string name);
}
